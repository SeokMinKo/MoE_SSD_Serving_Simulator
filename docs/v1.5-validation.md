# V1.5 validation and trust boundary

## Scope

- Baseline commit: `f4a5324d1957dbeb709871cbc6025b5b20ca5600`
- Topology-preset integration base: `5ccc81eb104af2217c949cbd6d7bc5594b52b482`
- Kimi K3 preset update base: `bda0b4309424bfd96bc8c8933b6e306ee1c8d74e`
- Bottleneck Advisor base: `5e8d182e2396f3fdf0afc4d26e938ac52981db1d`
- Storage I/O / Sweep Lab feature base: `d7d23845437105008cb876731fece692346665a9`
- Validation timestamp: 2026-07-31 18:11 KST
- Validation target: Bottleneck Advisor, Storage I/O trace, Parameter Sweep Lab, and scenario artifact V3 working tree; the resulting commit SHA is recorded in the release report
- Product class: **Estimated sensitivity simulator / Unvalidated Alpha**

V1.5 is intended for controlled, relative comparisons of synthetic configurations. It is not a calibrated hardware predictor.

## Automated gate

Commands executed from the repository root:

```bash
npm ci --ignore-scripts
npm run check
npm test
git diff --check
npm audit --audit-level=high
```

Observed results:

- JavaScript syntax: 18 files passed
- Node tests: 118/118 passed
- Browser VM App integrity wrapper: passed
- UI App integrity scenarios: 22/22 passed in an actual browser
- `git diff --check`: passed
- Dependency audit: 0 vulnerabilities
- Browser console: 0 messages, 0 JavaScript errors
- Hardware sweep: completed with finite metrics and expected SSD, PCIe, RAM, VRAM and compute sensitivity directions

The repository includes `.github/workflows/validate.yml` for Node 22 push/PR validation. A remote GitHub Actions result is only available after the changes are pushed; the local gate does not claim that remote CI has run.

## Actual-browser checks

Checked at `http://127.0.0.1:8877/`:

- 1280px viewport: client width 1265px, no horizontal overflow; the action toolbar remained sticky at viewport top after scrolling
- 1024px Chrome viewport: client/scroll width 1009px with 0 overflow; sticky toolbar top remained 0px after 1200px scroll, Sweep dialog stayed within 17–992px, and initial focus entered parameter search
- Parameter help: 80 simulation controls and 80 native help buttons; focus/click opened the tooltip, Escape closed it, and help buttons were outside labels
- Sweep dialog: focus entered the search field and returned to `Open sweep lab`; Escape/Close were blocked while active and focused Cancel
- OAT: two custom SSD points completed 2/2; semantic progress reached 2/2; baseline plus two rows and non-empty TTFT canvas pixels rendered
- Grid: requested 100 combinations, retained the deterministic first 50, disclosed 50 omitted, and completed 50/50
- Cancel: stopped at a scenario boundary with one completed row retained and no later row added
- CSV: RFC 4180 output contained engine/mode/omitted/product-boundary metadata; the temporary link and Blob URL were cleaned up after click
- Storage selectors: all 3 X modes × 4 Y modes rendered 65 finite rows; tabs showed one panel, stacked three, and normalized overlay three with disclosure
- V3 completed sweep: two rows replayed; tampered definition total was rejected before UI application
- Worker replay: a valid 50-row, 86,465-byte completed sweep replayed off the main thread in 29ms while 8 one-millisecond UI heartbeat ticks continued
- Sweep simulation: canonical baseline and retained scenarios ran in a dedicated Worker with a 30-second per-run budget and user cancellation; default auto-placement completed-sweep export preserved baseline provenance
- Sweep pause/resume race probe: immediate resume during an in-flight scenario produced exactly rows `[0, 1]` with three Worker calls total (baseline plus two scenarios), without duplicate or skipped rows
- Successful V3 import: applied the Worker-verified replay result without a second main-thread simulation; browser instrumentation recorded 0 main-thread `simulate()` calls
- Import serialization: a newer concurrent import aborted the older replay Worker and remained the final UI state; sweep preparation blocked imports, and imports blocked new sweeps
- Superseded file reads: an older delayed `file.text()` rejection was converted to `AbortError` after a newer successful import and did not overwrite its Run ID or UI
- Worker requirement: with `Worker` unavailable, import failed closed before replay with 0 main-thread simulation calls
- Replay transfer bound: per-request serving token arrays are removed after Worker verification, before structured clone; the complete `{ artifact, replayResult }` envelope has an explicit 4MB JSON-byte budget, and unknown config/memory/placement extension fields fail closed
- Clone-safe replay state: non-UI `Map`, `Set`, and pending state arrays are removed before envelope measurement, and any remaining collection is rejected rather than undercounted
- Mode-specific artifact schema: Colibri fields cannot carry AFM payloads and AFM fields cannot carry Colibri payloads; placement metadata requires its exact policy-specific finite scalar shape
- Canonical artifact replay: export takes the runner-resolved `result.c`; import requires replay config equality, replay/artifact Run ID equality, integer completed-token counts, and canonical placement metadata
- Aggregate replay budget: top-level, completed-sweep baseline, and every retained row contribute to a pre-replay work cap, so a self-exported artifact cannot require more work than the Worker's 30-second import boundary permits
- Maximum accepted routing probe: Colibri `output=1024`, `conc=64`, `experts=4096`, `layers=1`, `active=1`, `corr=0` completed in 665.23ms after deterministic Zipf CDF precomputation (previous per-pick full scans exceeded the 30-second import budget)
- Concurrent complexity: validation models sparse-routing worst-case fallback work and actual per-layer warm-cache entries across initial and repeated traces; a measured 251ms warm-cache case remains accepted while a measured 23-second sparse-routing shape fails closed before it can be multiplied by artifact replay
- Route cardinality: sparse routes use bounded rejection sampling and dense/remaining routes use an O(n)-built Fenwick-tree weighted sampler without replacement; legal RNG boundaries `0`, `Number.MIN_VALUE`, and `1-Number.EPSILON` all fill 4,096 unique in-range experts without ascending-ID fallback bias
- Dense route probe: `experts=4096`, `active=4095`, `output=64` completed in 145.75ms; the former guarded sampler took 5.88 seconds for the same 64-token shape and implied more than 30 seconds at 1,024 tokens
- Atomic import: a tampered replay metric was rejected without changing prompt, seed, or current Run ID; a valid `sweep: null` artifact cleared prior completed sweep state
- Sweep editor: Custom strategy and `1,2,3` values survived parameter-search rerendering
- OOM result presentation: table status remained `oom`, all five unavailable performance cells rendered `—`, and no persisted finite placeholder was shown
- App integrity independence: 22/22 passed while the edited 1-token prompt, 2-token output, reduced topology, non-default seed, and current Run ID remained unchanged

- Deterministic current Run ID: `sim-7016d222`
- Export → parse → apply → replay Run ID: unchanged
- Replay metric deltas: TTFT 0, TPOT 0, throughput 0, peak memory 0
- Invalid `ssdBW=-1`:
  - fail-closed warning shown
  - KPI reset to `—`
  - playback stopped
  - token and accessible trace cleared
  - status remained `Invalid result` after 700ms
  - performance and memory canvas pixel sums both became 0
- Empty required Prompt input: rejected with a structured `prompt` validation error
- Concurrent auto placement (`conc=2`): applied VRAM/DRAM cache budgets exactly matched placement computed with the full request count
- Colibri no-contention scheduler parity probe:
  - analytic TTFT / event TTFT: 952.822222ms / 952.822222ms
  - analytic total latency / event total latency: 985.182222ms / 985.182222ms
- Accessible labels: 79 connected labels; visible named controls had no unnamed items
- Canvas alternatives: both canvases have image roles/names and the result includes a token-level HTML trace table
- 375px iframe:
  - inner width 371px; document width 356px; no horizontal overflow
  - number input 44px; checkbox target 44px; Run button 49px
  - Run controls remain in normal document flow and do not cover preset provenance or calibration warnings
- `prefers-reduced-motion` disables cursor animation

### Published topology preset checks

- Source dataset rows: 31
- Eligible presets with disclosed MoE layers, routed Experts/layer, and routed top-k: 29
- Programmatic CSV-to-preset comparison: all retained metadata and source fields matched; 0 mismatches
- Repository-local provenance gate: `tools/check-presets.cjs` validates `data/moe_model_trend_with_layers_2026-07-21.csv` on every `npm run check`
- Excluded incomplete rows: Qwen3.7-Max, Qwen3.8-Max-Preview
- Actual browser selector: 30 options (Custom + 29 models); desktop width 355px in a 385px panel
- 375px iframe preset control: document/client widths both 360px with no horizontal overflow; summary y=693–825, Run controls y=3348–3408, overlap 0px
- DeepSeek-V3 / R1 application:
  - MoE layers: 58
  - Experts/layer: 256
  - Active routed Experts/token: 8
  - Expert size, resident weights, KV size, host capacity, and SSD bandwidth remained unchanged
- Kimi K3 official open-weight/config application: 92 MoE layers, 896 Experts/layer, top-k 16; non-topology controls remained unchanged
- Browser App integrity test uses its canonical 75/256/8 fixture and remains 22/22 after selecting Kimi K3
- Manual topology input changed the selector back to Custom
- Preset summary exposed the supplied source config and explicitly labeled the mapping `Topology only`
- Preset summary displayed the supplied disclosure status
- Non-HTTPS source URLs are not rendered as links
- Browser console after selection and manual editing: 0 messages, 0 JavaScript errors

### Bottleneck Advisor checks

- Four scorecards rendered directly below the KPI row: Prefill, first token, decode, memory pressure
- Each scorecard exposed Storage, Data movement, Compute, and Capacity / policy as independent 0–100 relative-pressure scores
- Default Colibri browser result:
  - Prefill: Storage 84, Data movement 100, Compute 12, Capacity 80
  - First token: Storage 42, Data movement 45, Compute 21, Capacity 80
  - Decode: Storage 69, Data movement 48, Compute 23, Capacity 80
  - Memory pressure: Storage 0, Data movement 7, Compute 0, Capacity 80
- Every resource detail exposed its deterministic formula, numerator/denominator evidence, setting direction, condition, and trade-off
- Concurrent Prefill, first-token, and decode evidence used phase-local event-scheduler SSD/PCIe/DRAM/compute queue contention; aggregate Prefill queue was not attributed to Decode
- One-token runs kept the Decode card but reported it unavailable with four zero scores instead of duplicating first-token evidence
- Colibri/AFM timing cards used exact per-job `StorageResource` service/queue evidence; layer jobs were not collapsed into a reconstructed QD latency wave
- AFM periodic patch/materialization time was attributed to Data movement rather than Compute
- Concurrent AFM patch-only Prefill and Decode work used the dedicated shared patch resource: Compute busy/queue and Compute score remained zero while Data movement exposed patch contention
- Memory-pressure Storage evidence separated initial pre-decode swap service from token-phase swap service and used total completed run elapsed as its denominator
- AFM browser result rendered all four phase contracts without invalid values
- Empty required Prompt input cleared stale KPI and Advisor scores and showed `Unavailable: configuration validation failed`
- `moe-ssd-sim/v3` export contained the derived Advisor snapshot and optional sweep snapshot; plausible in-range Advisor and sweep metric tampering was rejected during deterministic replay
- 375px iframe: 371px viewport, 356px client width, 356px scroll width, no horizontal overflow; Advisor used one 298px column with four phase cards and 16 resource details
- Actual browser App integrity test: 22/22; browser console: 0 messages, 0 JavaScript errors

## Deterministic probes: baseline → V1.5

| Probe | `f4a5324` | V1.5 working tree |
|---|---:|---:|
| 1GB Expert-cache physical use | 1.012900390625GB | 0.9937890625GB |
| First-token previous-route prefetch issued | 3 | 0 |
| Swap-out bytes kept pending during write | not represented | 0.000186368GB in probe |
| AFM boundary, double buffer on/off | not captured | 1030.568ms / 1035.568ms |
| One-request event throughput | not implemented | 33.1345 tok/s |
| Two-request event throughput | legacy aggregate multiplication | 33.1345 tok/s |
| Two/one ratio | not contention-derived | 1.0000× in this SSD-bound probe |
| Two-request SSD queue delay | not represented | 20.0356ms |

These values are deterministic synthetic probes, not measured device performance.

## V1.5 invariants covered by regression tests

- cache byte capacity includes packing overhead
- safe integer parsing and fail-closed validation of all calculation fields
- threshold order and relational constraints are rejected rather than normalized
- invalid render clears stale KPI, tables, charts and playback state
- swap-out source residency remains until write completion
- swap-in waits reap completed pending page-outs before new pressure accounting
- AFM pipelined overlap requires double buffering
- previous-token prefetch has no first-token future-route oracle
- evicted prefetch entries cannot later be counted as useful
- stable same-time event ordering
- shared SSD/DRAM/PCIe/compute resource contention
- single-request Colibri and AFM timelines preserve the analytic no-contention baseline
- swap I/O is included in scheduler SSD work
- AFM selector/initial patch/prefill are included in scheduler TTFT
- data-ready requests inside the admission window batch together
- malformed requests and oversized scenario artifacts are rejected
- import Run ID must match canonical config and request shape
- deterministic import replay must reproduce every persisted numeric metric, OOM status and model-status value
- scenario V3 validates four-phase Advisor structure and recomputes the derived insight and every retained sweep point during import replay
- Advisor scores remain deterministic integers in 0–100 and distinguish storage-bound, compute-bound, concurrent queue, invalid-input and partial-OOM probes
- concurrent auto placement reserves KV capacity using the full request count
- empty required numeric controls are rejected rather than coerced to zero
- topology presets contain unique IDs, safe positive integer routing dimensions, `active <= experts`, and HTTPS provenance
- preset application changes only MoE layers, routed Experts/layer, and routed top-k
- manual edits and scenario imports clear stale published-model attribution
- 30-scenario finite/resource-bound property matrix

## Explicit trust boundary

V1.5 does **not** validate or support claims about:

- absolute TTFT, TPOT, TPS, SLA, tail latency or capacity planning
- hardware purchase comparisons
- kernel-level GPU continuous batching
- request fairness or production scheduler semantics
- cross-request Expert-cache residency sharing
- in-flight Expert-read coalescing
- GPU VRAM bandwidth, page migration or kernel launch/graph behavior
- OS page-level VM behavior
- an empirical SSD QD/read-size service curve
- performance calibration inferred from a topology preset or total/active parameter counts
- Bottleneck Advisor scores as probabilities, measured utilization guarantees, real-device root-cause diagnosis, or predicted improvement percentages

SSD QD currently affects command-latency waves under one aggregate bandwidth cap; it is not a model of actual outstanding slots. Real runtime traces, device service-curve calibration and hold-out validation are required before using absolute metrics.

Scenario replay, derived Advisor replay, and the UI App integrity test execute the same JavaScript model and are regression checks, not independent physical oracles.
